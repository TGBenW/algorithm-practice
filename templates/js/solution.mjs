// solution.mjs — minimal JavaScript template (Node.js)
// Fill in your function, then add a quick manual run at the bottom if needed.

// Example signature (edit to your task):
export function solve(...args) {
  // TODO: implement
  return null;
}

// --- Manual run (optional) ---
// Run: node path/to/js/solution.mjs
if (import.meta.url === `file://${process.argv[1]}`) {
  // Example:
  // const result = solve([2,7,11,15], 9);
  // console.log(result);
  console.log("Tip: add a small demo call above to print results.");
}
